name: Kamino Portfolio

on:
  workflow_dispatch: {}

jobs:
  run-kamino:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 24

      - run: npm ci

      - run: npm run kamino
